npm create vite@latest react_project
Select a framework: React 
Select a variant: JavaScript

RUN:
  cd react_project
  npm install
  npm run dev

Update versions:
  // npm install vite@5.4.11 - Do not as already default latest version is greater than this.
  // npm install react@19.0.0 react-dom@19.0.0 - Do not as already default latest version is greater than this.
  npm install vite-plugin-eslint --save-dev
